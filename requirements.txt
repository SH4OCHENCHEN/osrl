# COMVA runtime dependencies.
#
# Recommended Anaconda setup:
#   conda create -n comva python=3.10 -y
#   conda activate comva
#   # Your driver reports CUDA driver API 12.8, so avoid cu129/cu130 PyTorch.
#   # Recommended inside the conda env:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# Conda-only fallback if pip wheels are inconvenient:
#   conda install pytorch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 pytorch-cuda=12.4 -c pytorch -c nvidia
#
# If you use CPU only:
#   conda install pytorch cpuonly -c pytorch

# Core numerical / training stack
numpy>=1.23
pandas>=1.5
matplotlib>=3.7
tqdm>=4.65
loguru>=0.7
wandb>=0.15

# Environments and offline RL datasets
gymnasium>=0.29
gym==0.23.1
dsrl
huggingface-hub>=0.20

# Plotting and representation analysis tools
scikit-learn>=1.2
umap-learn>=0.5

# Optional packages for local custom environments.
# Install these only if you use the corresponding utils/environments modules.
# mujoco>=2.3
# mujoco-py==2.1.2.14
# metadrive-simulator
# bullet-safety-gym
#
# The code imports `diffuser.environments.wrappers` in several legacy custom
# environments. Install your local/third-party `diffuser` package if you need
# those environments.
